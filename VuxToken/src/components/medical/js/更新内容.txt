2020.4.xx
优化Elementui的日期控件,将弹出的日期置于屏幕中,隐藏页面头部标题以实现目的.

2020.5.xx
全部: 优化药品耗材列表 将筛选类型框改为树形菜单,增加生成树形菜单的后台方法.


2020.6.xx 
深圳 将送往地点和出车分站控件改为输入部分文字后可模糊查询相关数据的下拉菜单. 目前会有输入后没有选下拉值而保存非
字典值的情况待改进.



2020.7.6  
深圳 
1.将救治措施模块打包医嘱的方法优化, 监听器触发方法因为SaveDoctorOrders方法最后会重置绑定之而无限循环保存方法.
去掉监听器的触发保存事件改用@on-change='SaveDoctorOrders(DoctorOrder)'.

2.el-table 增加 fit="true"属性用于自适应各种分辨率的设备  min-width="5%" 用于将每列的最小百分比设置

2020.7.9
深圳
1.将现病史换为<x-textarea>组件
2.将获取医嘱字典的接口及DAL层方法重写，由前台直接传入id省去在控制层查找id的代码，通过这种方式解决跨域的问题。



2020.7.15
深圳
1.将药品耗材的删除方法（VuxdeleteGoods）从data调整为params这样可以将跨域的问题解决。
2.将药品耗材表单页面在返回救治记录时的默认提交去掉，否则会提交两次。
3.将药品耗材表单页面的保存耗材方法（VuxSaveDetailRecord）涉及到insert的操作，option预检的请求会重复执行一次
所以会连续添加两条，使用 data:"msg=" + encodeURIComponent(JSON.stringify(data2)),会变为简单请求不发出
预检请求。
4.深圳的vue代码中将基础信息中的时间控件加入了校验（当填写了内容后会将控件变为不可编辑状态）。
5.考虑把基础信息中的获取任务信息做判断（若任务类型没有填则自动获取任务信息）。


2020.7.17
总体考虑把单选控件二次封装 加入点击左侧取消默认清空的功能（还未实现）
深圳： 将心脏改为多选按钮

2020.7.20
1.将应填未填数量计算函数拆分，修复了还未加载完毕先出数字的不利于体验bug。
☆2.将整体前后端分离通过后端验证的方式从使用padlogin登录直接改变为获取padsession作为token验证登录，
省去了登录步骤将明文用户名密码规避。相应修改了病历模块BaseController并将预检请求做统一的过滤规避预检请求执行业务代码的bug。
3.发现了当post请求在请求拦截其中设为config.headers['Content-Type'] = 'application/x-www-form-urlencoded'时
会导致服务端控制层获取不到参数的bug，将其注释掉即可。


2020.7.21-7.28

☆1.尝试用localStorage存放M_PredefinedTreatmentRecord表体格检查字典减少请求数量.
2.二次封装了vux popup-picker组件 ,实现了目前病历对该组件的所有需求,但是初始化较慢.会出现打开页面一秒后再显示的问题.



2020.7.29
目前开始尝试将现有的字典获取全部替换为从localStorage中获取

2020.7.30-31
将更改跨域策略后的源码部署到线上后发现跨域仍然报错，故改回isajax=True 的模式

2020.8.3
将vue源码放入github仓库中管理 Edsion-Lsy WTF_lsy199632

测试
